#include "GlobalConstants.h"

extern mat4 transMatrix = mat4(1.0f);
extern mat4 scaleMatrix = mat4(1.0f);
extern mat4 rotMatrix = mat4(1.0f);
extern float scalar = 1.0f;
extern bool rightClickPressed = false;
extern vec2 startPosition = vec2(1.0f);
extern vec2 endPosition = vec2(1.0f);